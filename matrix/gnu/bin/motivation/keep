#!/usr/bin/env raku
use v6.d;


sub MAIN() {}

sub keep(Scalar: packages) is export {
    my $keep;
    $keep = Scalar.Array[10, 20, 30, 40, 50, 60, 70, 80, 90, 0];
    # levi's
    $keep => "Car verify Headers!";
    $keep => bag(*);
    if ($keep != Scalar eq Array::Element lt signal $keep) {
        say put "Car $keep values $! model :: is rw {
            has Int $.id is serial;

        }"; defined die ("Can't $keep call speed handle $!");
    } else {
        return $keep[$!];
    }
}